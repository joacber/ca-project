version: 2.0
jobs:
  test:
    docker:
      - image: circleci/python:3.6.1


    working_directory: ~/repo

    steps:
      - checkout

      # Download and cache dependencies
      - restore_cache:
          keys:
            - v1-dependencies-{{ checksum "requirements.txt" }}
            # fallback to using the latest cache if no exact match is found
            - v1-dependencies-

      - run:
          name: install dependencies
          command: |
            python3 -m venv venv
            . venv/bin/activate
            pip install -r requirements.txt

      - save_cache:
          paths:
            - ./venv
          key: v1-dependencies-{{ checksum "requirements.txt" }}

      # run tests!
      - run:
          name: run tests
          command: |
            . venv/bin/activate
            python tests.py

      - store_artifacts:
          path: test-reports
          destination: test-reports
  package:
    docker:
      - image: alpine:3.7


    steps:
      - checkout
      - add_ssh_keys:
         fingerprints:
          - "b6:6c:92:21:85:88:1e:95:3a:10:81:3a:dc:22:52:21"
      - run: apk add openssh
      - run:
          name: Deploy Over SSH
          command: |
            ssh ubuntu@34.77.143.146 "~/code/ca-project/compose.sh"

workflows:
  version: 2
  job_flow:
    jobs:
    - test
    - package:
        requires:
        - test
